<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme -->
    <style name="Base.Theme.WaifuCollection" parent="Theme.Material3.DayNight">
        <!-- Ubah ActionBar style -->
        <item name="actionBarStyle">@style/CustomActionBarStyle</item>
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.WaifuCollection" parent="Base.Theme.WaifuCollection" />

    <!-- Custom ActionBar style -->
    <style name="CustomActionBarStyle" parent="Widget.AppCompat.ActionBar">

        <item name="background">@color/pink</item>
        <item name="titleTextStyle">@style/CustomActionBarTitleTextStyle</item>
    </style>
        <style name="CustomActionBarTitleTextStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
            <item name="android:textColor">@android:color/white</item>
        </style>
</resources>
